name: Capture AI and Muuqal screenshots

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium

      - name: Capture live project screenshots
        run: |
          mkdir -p captured-screenshots
          cat > capture-projects.mjs <<'EOF'
          import { chromium } from 'playwright';

          const projects = [
            {
              name: 'ai-interview-coach',
              url: 'https://ai-interview-coach-sigma-bay.vercel.app/',
              wait: 5000,
            },
            {
              name: 'muuqal-construction-real-estate',
              url: 'https://muuqal-construction-real-estate.vercel.app/',
              wait: 8000,
            },
          ];

          const browser = await chromium.launch({ headless: true });
          const page = await browser.newPage({
            viewport: { width: 1600, height: 900 },
            deviceScaleFactor: 1,
          });

          for (const project of projects) {
            await page.goto(project.url, {
              waitUntil: 'domcontentloaded',
              timeout: 90000,
            });
            await page.waitForTimeout(project.wait);
            await page.screenshot({
              path: `captured-screenshots/${project.name}.png`,
              fullPage: false,
            });
          }

          await browser.close();
          EOF
          node capture-projects.mjs

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        with:
          name: ai-muuqal-screenshots
          path: captured-screenshots
          if-no-files-found: error
